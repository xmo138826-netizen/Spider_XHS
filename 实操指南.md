# Spider_XHS 实操指南

> 本文档记录了 Spider_XHS 项目所有功能的实际操作方法和注意事项。

---

## 🚀 下次如何快速启动

### 哪些是永久配置（只做一次）

以下操作**只需要做一次**，之后启动项目无需重复：

| 操作 | 一次性？ | 说明 |
|------|----------|------|
| 安装 Python | ✅ 一次 | 只要没卸载就行 |
| 安装 Node.js | ✅ 一次 | 只要没卸载就行 |
| `npm install` | ✅ 一次 | Node 依赖（crypto-js、jsdom）装一次即可 |
| `pip install -r requirements.txt` | ✅ 一次 | Python 依赖装一次即可 |
| 克隆项目代码 | ✅ 一次 | 除非删掉重建 |

### 哪些需要重复操作

| 操作 | 频率 | 说明 |
|------|------|------|
| **更新 Cookie** | 每次启动前检查 | 写文案时会给出，你只需贴进 `.env` 文件 |
| 获取笔记 URL | 每次爬取前 | 从浏览器打开笔记，复制完整链接（含 xsec_token） |
| 获取用户主页 URL | 每次爬取前 | 从浏览器打开用户主页，复制完整链接 |
| 关闭系统代理 | 发布前临时 | 创作者平台发布时需要（发布完可恢复） |

### 30 秒启动流程

```bash
# 1️⃣ 进入项目目录
cd "g:/Github projiect/Spider_XHS"

# 2️⃣ 更新 Cookie（如果你说"帮我更新 cookie"，我会给你链接，你贴进 .env 就行）
# 打开 .env 文件，替换 COOKIES='...' 那一行

# 3️⃣ 验证环境
python -c "from xhs_utils.common_util import init; c, b = init(); print('环境 OK, Cookie 长度:', len(c))"

# 4️⃣ 开始使用
# 然后告诉我你要爬哪个笔记 / 搜什么关键词 / 发布什么内容
```

### 获取新 Cookie 的方法

当 Cookie 过期时（通常 24-48 小时），按以下步骤更新：

1. **浏览器打开** www.xiaohongshu.com 并登录
2. **F12** → **应用（Application）** → **Cookie** → **www.xiaohongshu.com**
3. 手动找到以下关键字段复制（按冒号分隔，连成一行）：

```
a1=xxx; webId=xxx; gid=xxx; web_session=xxx; ...
```

> ⚠️ 如果 Cookie 过期了，采集会失败（返回登录页），发布也会失败。到时就更新 Cookie 即可。

---

## 📋 目录

1. [下次如何快速启动](#-下次如何快速启动)
2. [环境准备](#环境准备)
3. [Cookie 配置](#cookie-配置)
4. [数据采集（PC 端）](#数据采集pc-端)
5. [内容发布（创作者平台）](#内容发布创作者平台)
6. [企业功能（蒲公英/千帆）](#企业功能蒲公英千帆)
7. [常见问题](#常见问题)
8. [功能速查表](#功能速查表)

---

## 环境准备

### 必需环境

- **Python**: 3.10+
- **Node.js**: 20+
- **系统**: Windows/macOS/Linux

### 安装依赖

```bash
# 安装 Python 依赖
pip install -r requirements.txt

# 如果 pip 安装太慢（opencv-python 较大），可分开装
pip install PyExecJS requests loguru python-dotenv retry openpyxl aiohttp qrcode
pip install opencv-python numpy

# 安装 Node.js 依赖
npm install
```

### 目录结构

```
Spider_XHS/
├── spider/spider.py          # 爬虫入口（可直接运行）
├── apis/                     # API 封装
├── xhs_utils/                # 工具函数
├── static/                   # JS 签名核心文件
├── .env                      # Cookie 配置
└── datas/                    # 数据保存目录
    ├── media_datas/          # 图片/视频下载
    └── excel_datas/          # Excel 导出
```

---

## Cookie 配置

### 获取 Cookie 方法

1. **浏览器登录小红书**（www.xiaohongshu.com）
2. **按 F12 打开开发者工具**
3. **切换到「网络」（Network）标签**
4. **刷新页面，找到任意请求**
5. **复制请求头中的 `cookie` 字段**

### 配置到项目

将 Cookie 复制到项目根目录的 `.env` 文件：

```bash
COOKIES='你的完整cookie字符串'
```

> **注意**：Cookie 有时效性，失效后需重新获取。建议每天更新一次。

### Cookie 包含的关键字段

有效的 Cookie 应包含以下字段（至少）：

| 字段 | 说明 | 必需 |
|------|------|------|
| `a1` | 签名密钥 | ✅ |
| `webId` | 设备标识 | ✅ |
| `gid` | 设备指纹 | ✅ |
| `web_session` | PC 端会话 | ✅ |
| `x-user-id-creator.xiaohongshu.com` | 创作者平台用户 ID | 创作者功能必需 |
| `access-token-creator.xiaohongshu.com` | 创作者平台令牌 | 创作者功能必需 |

---

## 数据采集（PC 端）

### 实操 1：爬取单条笔记详细信息

**功能**：获取一篇笔记的完整信息（标题、作者、图片、视频、互动数据等）

```python
from apis.xhs_pc_apis import XHS_Apis
from xhs_utils.common_util import init
from xhs_utils.data_util import handle_note_info, download_note

cookies_str, base_path = init()
xhs_apis = XHS_Apis()

note_url = 'https://www.xiaohongshu.com/explore/笔记ID?xsec_token=...'
success, msg, note_info = xhs_apis.get_note_info(note_url, cookies_str)

if success:
    note_data = note_info['data']['items'][0]
    note_data['url'] = note_url
    result = handle_note_info(note_data)
    # 下载图片
    download_note(result, base_path['media'], 'media-image')
```

**返回数据包括**：
- 笔记 ID、URL、类型（图集/视频）
- 作者信息（昵称、头像、用户 ID）
- 标题、描述
- 互动数据（点赞、收藏、评论、分享）
- 标签、发布时间、IP 属地
- 图片列表（无水印）、视频地址

---

### 实操 2：搜索关键词获取笔记

**功能**：按关键词搜索笔记，支持多种排序方式

```python
from spider.spider import Data_Spider
from xhs_utils.common_util import init

cookies_str, base_path = init()
spider = Data_Spider()

query = "PPT 配色"
require_num = 10                    # 获取数量
sort_type_choice = 0                 # 排序：0=综合 1=最新 2=最多点赞 3=最多评论 4=最多收藏
note_type = 0                        # 类型：0=不限 1=视频 2=图集
note_time = 0                        # 时间：0=不限 1=一天内 2=一周内 3=半年内

spider.spider_some_search_note(
    query, require_num, cookies_str, base_path,
    'all',                          # 保存方式：all=全部 media=仅图片 excel=仅Excel
    excel_name=query,
    sort_type_choice=sort_type_choice,
    note_type=note_type,
    note_time=note_time
)
```

**输出**：
- 每篇笔记的详细信息
- 图片下载到 `datas/media_datas/`
- Excel 导出到 `datas/excel_datas/关键词.xlsx`

---

### 实操 3：爬取用户全部笔记

**功能**：获取指定用户的所有已发布笔记

```python
from spider.spider import Data_Spider
from xhs_utils.common_util import init

cookies_str, base_path = init()
spider = Data_Spider()

user_url = 'https://www.xiaohongshu.com/user/profile/用户ID?xsec_token=...'
spider.spider_user_all_note(user_url, cookies_str, base_path, 'all')
```

**自动完成**：
- 获取用户所有笔记列表
- 逐篇下载图片/视频
- 生成 Excel 报表

---

### 实操 4：获取主页推荐频道

**功能**：获取小红书主页的推荐频道和推荐笔记

```python
from apis.xhs_pc_apis import XHS_Apis
from xhs_utils.common_util import init

cookies_str, base_path = init()
xhs_apis = XHS_Apis()

# 1. 获取所有频道
success, msg, channels = xhs_apis.get_homefeed_all_channel(cookies_str)
cats = channels['data']['categories']

for cat in cats:
    print(f"频道: {cat['name']} (id: {cat['id']})")

# 2. 获取推荐笔记
category = 'homefeed_recommend'  # 频道 ID
success, msg, notes = xhs_apis.get_homefeed_recommend(
    category, '', 1, 0, cookies_str
)
```

**11 个推荐频道**：
- 穿搭、美食、彩妆、影视、职场
- 情感、家居、游戏、旅行、健身、视频

---

### 实操 5：获取笔记评论

**功能**：获取指定笔记的所有评论

```python
from apis.xhs_pc_apis import XHS_Apis
from xhs_utils.data_util import handle_comment_info
from xhs_utils.common_util import init

cookies_str, base_path = init()
xhs_apis = XHS_Apis()

note_url = 'https://www.xiaohongshu.com/explore/笔记ID?xsec_token=...'
success, msg, comment_list = xhs_apis.get_note_all_comment(note_url, cookies_str)

for comment in comment_list:
    result = handle_comment_info({
        'note_id': '笔记ID',
        'note_url': note_url,
        'id': comment['id'],
        'user_info': comment['user_info'],
        'content': comment['content'],
        'show_tags': comment['show_tags'],
        'like_count': comment['like_count'],
        'create_time': comment['create_time'],
        'ip_location': comment['ip_location'],
        'pictures': comment['pictures'],
    })
    print(f"{result['nickname']}: {result['content']}")
```

---

## 内容发布（创作者平台）

### ⚠️ 重要：系统代理问题

**创作者平台 API 会读取系统代理设置**。如果你的系统开了代理（如 `127.0.0.1:10808`），请求会失败。

**临时解决方法**（每次发布前）：
1. 按 `Win + I` 打开设置
2. 进入「网络和 Internet」→「代理」
3. 关闭「使用代理服务器」
4. 发布完成后可重新开启

### 实操 6：查看已发布作品

```python
from apis.xhs_creator_apis import XHS_Creator_Apis
from xhs_utils.common_util import init

cookies_str, base_path = init()
creator = XHS_Creator_Apis()

success, msg, result = creator.get_publish_note_info(1, cookies_str)
if success:
    notes = result['data']['notes']
    for note in notes:
        print(f"{note['display_title']} | 点赞: {note['liked_count']}")
```

---

### 实操 7：发布图集笔记

**功能**：上传 1-9 张图片并发布为图集笔记

```python
from apis.xhs_creator_apis import XHS_Creator_Apis
from xhs_utils.common_util import init

cookies_str, base_path = init()
creator = XHS_Creator_Apis()

images = [
    'g:/path/to/image1.png',
    'g:/path/to/image2.png',
    # ... 最多 9 张
]

post_data = {
    'title': '笔记标题',
    'desc': '笔记描述，可包含话题标签 #话题',
    'media_type': 'image',
    'images': images,
    'topic_ids': [],        # 话题 ID（可选）
}

success, msg, result = creator.post_note(post_data, cookies_str)
if success:
    print(f"发布成功！笔记链接: {result['share_link']}")
```

**发布参数说明**：

| 参数 | 说明 | 示例 |
|------|------|------|
| `title` | 笔记标题 | 最多 20 字 |
| `desc` | 笔记描述 | 支持 #话题标签 |
| `media_type` | 媒体类型 | `image` 或 `video` |
| `images` | 图片路径列表 | 绝对路径，最多 9 张 |
| `topic_ids` | 话题 ID 列表 | 可选，留空自动识别 |
| `type` | 可见性 | `0`=公开 `1`=仅自己可见 |

**⚠️ 关于「仅自己可见」**：

之前代码的 `type` 默认值是 `1`（仅自己可见），已修复为 `0`（公开）。如果你想要公开发布，可以：
- 确保使用最新代码（已修复）
- 或显式传入 `type=0`

---

### 实操 7.2：发布视频笔记

```python
post_data = {
    'title': '视频笔记标题',
    'desc': '视频描述',
    'media_type': 'video',
    'video': 'g:/path/to/video.mp4',  # 视频文件路径
    'images': [],  # 视频笔记不需要图片
}

success, msg, result = creator.post_note(post_data, cookies_str)
```

**视频发布流程**：
1. 上传视频 → 获取 video_id
2. 自动提取封面 → 上传封面
3. 等待转码完成
4. 创建笔记

---

## 企业功能（蒲公英/千帆）

> 以下功能需要**企业账号认证**，普通 Cookie 无法访问。

### 实操 8：蒲公英平台 - KOL 数据

蒲公英是小红书的**品牌合作平台**，提供 KOL 博主数据查询功能。

**需要权限**：
- 品牌账号认证
- 蒲公英平台登录 Cookie（`pgy.xiaohongshu.com`）

**实操过程**：

```python
from apis.xhs_pugongying_apis import PuGongYingAPI
from xhs_utils.common_util import init

cookies_str, base_path = init()
pgy = PuGongYingAPI()

# 1. 获取所有类目
categories = pgy.get_all_categories(cookies)

# 2. 按类目获取 KOL 列表
user_list, total = pgy.get_user_by_page(1, cookies, contentTag=None)

# 3. 获取博主详细数据
kol_detail = pgy.get_user_detail(kol_user_id, cookies)

# 4. 获取博主粉丝画像
fans_data = pgy.get_user_fans(kol_user_id, cookies)

# 5. 获取博主历史趋势
trend_data = pgy.get_user_trend(kol_user_id, cookies)
```

**KOL 数据包括**：
- 基础信息：昵称、粉丝数、互动率
- 报价范围：图文/视频报价
- 粉丝画像：年龄、性别、地域分布
- 历史数据：30天/90天互动趋势

---

### 实操 9：千帆平台 - 分销商数据

千帆是小红书的**分销商精选平台**。

**需要权限**：
- 企业账号认证
- 千帆平台登录 Cookie

**实操过程**：

```python
from apis.xhs_qianfan_apis import QianFanAPI
from xhs_utils.common_util import init

cookies_str, base_path = init()
qf = QianFanAPI()

# 1. 获取所有类目
categories = qf.get_all_categories(cookies)

# 2. 获取分销商列表
user_list = qf.get_some_user(choice, categories, 50, cookies)

# 3. 获取分销商详细数据
detail = qf.get_user_detail(distributor_user_id, cookies)
```

**分销商数据包括**：
- 基础信息：店铺名称、合作品类
- 合作数据：合作品类、店铺信息
- 商品信息：在售商品列表

---

## 常见问题

### Q1：Cookie 多久更新一次？

**建议**：每天更新一次。Cookie 时效通常为 24-48 小时。

### Q2：发布的内容「仅自己可见」怎么办？

**原因**：代码中 `type` 默认值设置错误。

**解决**：
- 已修复代码（默认改为 `0`= 公开）
- 或在调用时显式传入 `type=0`

### Q3：系统代理导致请求失败？

**症状**：`ProxyError` 或 `Connection aborted`

**解决**：
- 临时关闭系统代理（发布创作者平台内容时）
- 或使用 `proxies={}` 参数

### Q4：蒲公英/千帆平台返回空数据？

**原因**：普通 Cookie 无企业权限。

**解决**：
- 申请企业账号认证
- 使用蒲公英/千帆平台的专用 Cookie

### Q5：图片上传失败？

**检查**：
1. 图片路径是否为绝对路径
2. 图片格式是否支持（JPG/PNG）
3. `opencv-python` 是否安装

### Q6：笔记 URL 过期了怎么办？

**原因**：`xsec_token` 有时效性。

**解决**：
- 重新从浏览器获取最新的笔记 URL
- 或用笔记 ID 重新拼接

### Q7：如何批量下载笔记？

**方法**：
```python
from spider.spider import Data_Spider

notes = [
    'https://www.xiaohongshu.com/explore/ID1?token=...',
    'https://www.xiaohongshu.com/explore/ID2?token=...',
    # ...
]

spider = Data_Spider()
cookies_str, base_path = init()
spider.spider_some_note(notes, cookies_str, base_path, 'all', '批量下载')
```

---

## 📊 功能速查表

| 功能 | API 类 | 方法 | Cookie 要求 |
|------|--------|------|-------------|
| 爬取单条笔记 | `XHS_Apis` | `get_note_info()` | PC 端 Cookie |
| 搜索笔记 | `XHS_Apis` | `search_some_note()` | PC 端 Cookie |
| 爬取用户笔记 | `XHS_Apis` | `get_user_all_notes()` | PC 端 Cookie |
| 获取评论 | `XHS_Apis` | `get_note_all_comment()` | PC 端 Cookie |
| 发布图集 | `XHS_Creator_Apis` | `post_note()` | 创作者平台 Cookie |
| 发布视频 | `XHS_Creator_Apis` | `post_note()` | 创作者平台 Cookie |
| 查已发布 | `XHS_Creator_Apis` | `get_publish_note_info()` | 创作者平台 Cookie |
| KOL 数据 | `PuGongYingAPI` | `get_user_by_page()` | 蒲公英平台 Cookie |
| 分销商数据 | `QianFanAPI` | `get_some_user()` | 千帆平台 Cookie |

---

## 🔧 代码修复记录

### 修复 1：文件路径处理（2025-07-18）

**问题**：`get_file_info()` 无法处理文件路径字符串。

**修复**：增加路径自动读取逻辑。

**文件**：`apis/xhs_creator_apis.py:296`

### 修复 2：默认可见性（2025-07-18）

**问题**：发布的笔记默认「仅自己可见」。

**修复**：将 `privacy_type` 默认值从 `1` 改为 `0`。

**文件**：`apis/xhs_creator_apis.py:208`

---

## 📞 支持

如有问题，请：
1. 查看本文档的「常见问题」部分
2. 检查 Cookie 是否有效
3. 确认系统代理是否关闭
4. 查看项目 Issues

---

*最后更新：2025-07-18*
